/// <reference types="react" />
import { IncomingColors } from "../types";
type SponsoredProps = {
    theme: IncomingColors;
};
declare const Sponsored: ({ theme }: SponsoredProps) => JSX.Element;
export default Sponsored;
