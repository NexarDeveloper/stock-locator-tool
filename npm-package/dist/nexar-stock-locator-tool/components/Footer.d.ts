/// <reference types="react" />
import { IncomingColors } from "../types";
type FooterProps = {
    theme: IncomingColors;
};
declare const Footer: ({ theme }: FooterProps) => JSX.Element;
export default Footer;
